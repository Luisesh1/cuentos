<%= form_for([@story,@scene]) do |f| %>
  <% if @scene.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@scene.errors.count, "error") %> prohibited this scene from being saved:</h2>

      <ul>
      <% @scene.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%=f.label :titulo%>
  <%=f.text_field :titulo%>
  <%=hidden_field :trans, id:"trans"%>
  <div id="representacion" style="border-style: solid; height: 200px;">
  
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
 <%=render "words/fotoPanelBus"%>


 